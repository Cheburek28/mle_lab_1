pipeline {
    agent any
sdcasc
    environment {
        DOCKERHUB_CREDS=credentials('mle-template')
        LC_ALL = "en_US.UTF-8"
        LANG    = "en_US.UTF-8"
        LANGUAGE = "en_US.UTF-8"
    }

options {
        timestamps()
        skipDefaultCheckout(true)
	}
    stages {

        stage('Pull image'){
            steps{
                sh '''
                        docker pull cheburek28/mle_lab_1:latest
                '''
            }
        }

        stage('Run container'){
            steps{
                bat '''
                        docker run --name mle-template -p 80:5556 -d cheburek28/mle_lab_1:latest
                '''
            }
        }
	}

    post {
        always {
            bat 'docker stop mmle_lab_1'
        }
    }
}